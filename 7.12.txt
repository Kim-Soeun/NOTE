1. final 메소드는 오버라이딩 x, 오버로딩 o

2. protected 클래스는 다른 클래스에서 호출 x
하지만, 상속받은 자식 클래스는 호출 o(new 객체 생성 x, super 생성자 호출 가능)

3.
<클래스 자동변환>
-상속관계에 있는 클래스 사이에서 발생
-자식이 부모타입으로 변환
-타입 변환 후 부모 클래스에 선언된 필드와 메소드만 접근 가능
-하지만, 오버라이딩한 메소드는 자식 메소드에서 호출
- 변환 방식
 예) Car car = new Sedan();




